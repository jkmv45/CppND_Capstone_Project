#include <iostream>
#include <vector>
#include <array>

#include "eigen3/Eigen/Geometry"

#include "swarmAgent.hpp"
#include "environmentManager.hpp"

int main() {
    std::cout << "Hello World!" << "\n";

    // std::vector<SimObj*> test, test2;
    // SwarmAgent* agent1 = new SwarmAgent();
    // SwarmAgent* agent2 = new SwarmAgent();
    // SwarmAgent* agent3 = new SwarmAgent();

    // test.push_back(agent1);
    // test.push_back(agent2);

    // test2.push_back(agent1);
    // test2.push_back(agent3);
    // test2.push_back(agent2);

    // std::cout << "Size Not Equal: " << (test.size() != test2.size()) << std::endl;
    // std::cout << "Content Not Equal: " << (test != test2) << std::endl;

    return 0;
}